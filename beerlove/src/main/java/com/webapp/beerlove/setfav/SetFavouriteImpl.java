package com.webapp.beerlove.setfav;

import com.webapp.beerlove.homepage.Product;

public class SetFavouriteImpl implements SetFavourite {
    @Override
    public boolean isStarred(int id) {
       // if(Id == id && starred == true) {
        //    return true;
            // also to be added to the favourites page
       // }
        final boolean b = false;
        return b;
    }
}
